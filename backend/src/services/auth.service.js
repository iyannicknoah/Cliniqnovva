// Service layer for auth/staff-onboarding (spec section 5 / 6.1).
// Will own: Firebase custom-claim assignment (role/organizationId/branchId),
// invite-link generation + SMS/email dispatch, deactivation side-effects.
// Intentionally empty during project setup (Phase 0).

module.exports = {};
